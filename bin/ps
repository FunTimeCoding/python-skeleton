#!/usr/bin/env python3
from sys import argv, exit

from python_skeleton.python_skeleton import PythonSkeleton

if __name__ == '__main__':
    application = PythonSkeleton(argv[1:])
    exit_code = application.run()
    exit(exit_code)
